{
  "palindrome-number": [
    {
      "header": "def isPalindrome(x):",
      "chunks": [
        {
          "step_id": "Part 1",
          "prompt": "Write code that returns False for any negative value of x since a negative number cannot be a palindrome.",
          "expected_type": "code",
          "reference": "if x < 0: return False"
        },
        {
          "step_id": "Part 2",
          "prompt": "Using the absolute value of x, write code that constructs the reverse of the digits as a new integer you can compare against.",
          "expected_type": "code",
          "reference": "original = abs(x)\nreversed_num = 0\nwhile original > 0:\n    reversed_num = reversed_num * 10 + original % 10\n    original //= 10"
        },
        {
          "step_id": "Part 3",
          "prompt": "Using x and its reversed value, return whether x reads the same forwards and backwards.",
          "expected_type": "code",
          "reference": "return x == reversed_num"
        }
      ]
    }
  ],
  "two-sum": [
    {
      "header": "def twoSum(nums, target):",
      "chunks": [
        {
          "step_id": "Part 1",
          "prompt": "Using the lookup, find and return the indices of the two numbers that sum to target.",
          "expected_type": "code",
          "reference": "for i, num in enumerate(nums):\n    complement = target - num\n    if complement in nums[i+1:]:\n        return [i, nums[i+1:].index(complement) + (i + 1)]"
        },
        {
          "step_id": "Part 2",
          "prompt": "Create a dictionary mapping each number to its index.",
          "expected_type": "code",
          "reference": "num_to_index = {num: i for i, num in enumerate(nums)}"
        }
      ]
    }
  ]
}